# Session 2026-09-01-001

## Metadata
- UTC start: 2026-09-01 02:53:01 (approximate; log start)
- UTC end: 2026-09-01 03:00:03 (approximate; capture end)
- Platform: Linux / Proton
- OS: Wine/11.0 as exposed to the game
- Game AppID: `2918300`
- Build ID: `PortalWars2-CL-600100`
- Branch: `++PortalWars2+Release-3.1`
- Executable SHA-256: not captured
- Proton version: not independently recorded in supplied files
- Host/client role: client
- Game mode: Matchmaking TDM Bot Arena / post-match WinningCircle
- Player count: 8 combatants; 1 human + 7 bots (3 allied, 4 opposing)

## Purpose
Capture a normal matchmaking TDM Bot Arena session on the official pre-shutdown server infrastructure, including match entry, live gameplay networking, loadout changes, power-weapon use, match completion, MVP/post-match presentation, and disconnect behaviour. This session provides a reproducible pre-shutdown server-backed baseline for the ReMaverick preservation project.

## Procedure
1. Launch the Linux/Proton client.
2. Use normal matchmaking to enter a TDM Bot Arena match on the official service.
3. Play the match to completion.
4. Change loadout during the latter half of the match.
5. Pick up the Borealis when it first spawned and again after a later spawn.
6. Pick up the Orion approximately halfway to two-thirds through the match; exact timing is uncertain.
7. Use the Orion secondary fire once, probably near the end shortly before one death.
8. Win the match and reach the normal MVP/WinningCircle presentation.
9. Allow post-match statistics/progression/celebration UI to run before returning to the menu.

## Observations

### Match result and participant-confirmed actions
The participant reports a successful Team Deathmatch Bot Arena match with three bot teammates and four bot opponents. The human player won the match and received MVP. The participant also confirms that the extended barrel and recoil grip worked functionally despite attachment-related log warnings.

### Match entry and gameplay endpoint
The client browses directly to a remote gameplay endpoint on UDP port `30925`, sends `SendInitialJoin`, is `Welcomed by server`, and loads `MAP_Zenith/Zenith_Main` with `PortalWarsGameModeV2_BP_C`. The PCAP independently contains a long-lived bidirectional UDP flow to the same sanitised endpoint/port.

### Live UE networking
The active `GameNetDriver` uses Iris replication and logs `OodleNetworkHandlerComponent`, `DTLSHandlerComponent`, and `StatelessConnectHandlerComponent` during the gameplay connection.

### Endpoint/control-plane context
Before the gameplay connection, the PCAP records DNS activity for multiple Maverick services, server-status/AGA services, gaming-SDK services, and multiple `*.edgegap.net` names. The exact final game endpoint is not present as an obvious matching DNS answer in the captured DNS question/answer set. This suggests a control-plane or application-level endpoint-selection step, but the exact allocator remains unresolved.

### Loadout / inventory warnings
Attachment/component resolution warnings recur across inventory epochs, including `TargetComponent Not Found` and invalid/default loadout-slot handling. These warnings do not demonstrate visible attachment failure: the participant explicitly reports that the extended barrel and recoil grip worked.

### Runtime Blueprint warnings
The log emits `Divide by zero: Divide_DoubleDouble` 128 times. Occurrences span startup, active gameplay and post-match/loading activity. This establishes a real runtime Blueprint/script error condition, but not its user-visible impact or owning graph.

The scoreboard widget also attempts to access missing indices in `CurrentDisplayedEntries` while the array length is 1. The participant did not notice an obvious scoreboard/UI problem.

### Power weapons and Gameplay Cue breadcrumbs
The match exercises weapon-pickup and Gameplay Cue systems, including `BP_WeaponPickup_C`, `POW_WallHack`, weapon-effect activity, and a missing AkComponent for the sound event `play_sx_wpn_3p_merpow_prifle_projectile`. The participant confirms Borealis and Orion pickups and one Orion secondary-fire use, but the log does not prove that this particular audio event belongs to either weapon.

### Anti-cheat / Merlin
The client reports `OS=Wine/11.0`, loads Merlin-related functionality, and logs `ReportServerSessionManager` data while EOS separately reports that the anti-cheat client is unavailable. This corroborates the existing RedKard/Proton finding in a real server-backed arena session.

### Post-match
The client creates `WBP_PostMatchTransitionStinger`, loads `Default__EXP_PostGame_Generic_C`, adds the `WinningCircle` level instance, and starts confetti/fireworks/trail presentation before finally closing the gameplay connection. The participant confirms the normal MVP screen appeared, including their name, statistics/progression flow, and celebration effects. Some post-match audio/animation assets are missing according to Wwise/UMG logging, but the presentation itself completed.

## Artifacts
- `logs/game/2026-09-01-001-PortalWars2-sanitized.log`
- `logs/anticheat/2026-09-01-001-steam-relevant-sanitized.txt`
- `logs/networking/2026-09-01-001-pcap-summary.txt`
- `networking/discovery/2026-09-01-001-dns-queries.csv`
- `networking/gameplay/2026-09-01-001-game-udp-packets.csv`
- `captures/pcap/2026-09-01-001.md`
- `archive/2026-09-01-001/` supporting snippets, event indexes, hashes and extended analysis

Raw captures remain private and are not included in the repository upload bundle.

## Redactions
Public/private network-identifying values, local IP addresses, account identifiers, home-directory paths and other machine-specific values have been sanitised. The game endpoint is represented consistently as `GAME_ENDPOINT_01` in published text. Raw source hashes are retained for private provenance.

## AI assistance
- Tool/model: ChatGPT / GPT-5.6 Luna
- Task: Repository-guided evidence triage, cross-correlation of UE log / Proton trace / PCAP, finding comparison, sanitisation, and documentation.
- What was manually verified: Participant action/result details supplied in the session description; attachment functionality; interpretation boundaries for game endpoint attribution and post-match MVP presentation. Important findings remain subject to human review before promotion in the repository.

## Follow-up
- Repeat normal matchmaking TDM under the same platform conditions and compare endpoint selection and UE handshake timing.
- Correlate pre-`Browse` HTTPS/application traffic with the final endpoint in a fresh capture.
- Capture a video timestamp alongside the scoreboard, loadout switch and power-weapon actions to correlate runtime warnings with visible state.
- Compare native Windows and Proton server-backed anti-cheat/Merlin startup behaviour.
