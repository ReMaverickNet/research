# Tooling

Analysis and capture helpers used to process research material.

- `analysis/` — analytical scripts and notebooks
- `capture/` — capture helpers
- `parsing/` — log and packet parsing
- `utilities/` — general-purpose helpers

Tools should produce reproducible outputs and avoid embedding secrets or raw personal information.
