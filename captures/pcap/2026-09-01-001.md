# Capture 2026-09-01-001

A private full-machine PCAP was captured for this session. The raw `.pcapng` is intentionally not included in the repository bundle.

See `logs/networking/2026-09-01-001-pcap-summary.txt` and the filtered packet/DNS exports under `networking/`.

The capture contains unrelated host traffic in addition to the game, so published conclusions use process/game-log timing and stable flow correlation rather than assuming every DNS request belongs to the game.
