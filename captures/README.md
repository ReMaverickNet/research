# Captures

Capture metadata and derived analysis artefacts.

- `pcap/` — packet-capture metadata
- `wireshark/` — Wireshark analysis artefacts
- `etw/` — ETW captures
- `other/` — other capture formats

Keep raw sensitive captures private or sanitised before publication. See [capture guides](../docs/capture/).
